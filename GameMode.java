package pacman;
 /**
 * Enum to represent the different game mode options for the ghost's movement: CHASE, SCATTER, AND FRIGHTEN. This is
  * implemented in the pacmanGame class through a switch statement.
 */
public enum GameMode {
    CHASE,
    SCATTER,
    FRIGHTEN;
}
